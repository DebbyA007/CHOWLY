import { PassMenu } from "@/components/directions-3/one/menu";

// No server data: the menu comes from the client cache, warmed before the tag is pressed.
export default function PassMenuPage() {
  return <PassMenu />;
}
